# manage-library

Add/edit/search `data/product-library.json`.

## Rules
- `id` must be unique slug.
- Macros stored per 100g only.
- Servings stored as grams.
- Set `source` to one of: nutracheck-import, manual, label-photo, estimated.
- Update `last_used` when a product is logged.
